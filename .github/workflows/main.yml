name: CI

on:
  pull_request:
    types: labeled

jobs:
  add-comment:
    if: github.event.label.name == 'QA'
    runs-on: ubuntu-latest
    steps:
      - name: 'Post comment'
        run: |
          echo "${{ github.event }}";
